<%= form_for(@payment_journal) do |f| %>
  <% if @payment_journal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment_journal.errors.count, "error") %> prohibited this payment_journal from being saved:</h2>

      <ul>
      <% @payment_journal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
